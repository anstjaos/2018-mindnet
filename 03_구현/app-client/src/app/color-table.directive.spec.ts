import { ColorTableDirective } from './color-table.directive';

describe('ColorTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTableDirective();
    expect(directive).toBeTruthy();
  });
});
